// "use client";
// import React from "react";
// import wol from "wol";

// const RemoteControlButton = () => {
//   const macAddress = "00:1A:2B:3C:4D:5E"; // Replace with the actual MAC

//   const handleWakeUp = () => {
//     wol.wake(macAddress, (error: Error | null) => {
//       if (error) {
//         console.error("Error:", error);
//       } else {
//         console.log("Remote machine waking up...");
//       }
//     });
//   };

//   return <button onClick={handleWakeUp}>Wake Up Remote Laptop</button>;
// };

// export default RemoteControlButton;
